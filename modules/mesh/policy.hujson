// headscale v0.2x+ ACL (HuJSON). owners must be "user:<name>" or "group:<name>".
// phase-1: open mesh. tighten when members exist.
{
  "acls": [
    {
      "action": "accept",
      "src": ["*"],
      "dst": ["*:*"],
    },
  ],
}
